const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('BROWSER CONSOLE:', msg.text())); page.on('pageerror', err => console.log('BROWSER ERROR:', err.toString())); await page.goto('http://localhost:5010/ui-showcase/showcase-feedback'); await page.waitForSelector('app-button'); const btns = await page.('app-button'); for(let btn of btns) { await btn.click(); await new Promise(r => setTimeout(r, 200)); } await browser.close(); })()
