/**
 * Staging environment configuration
 * Used when running `ng build --configuration staging`
 */
export const environment = {
  production: false,
  appName: 'Atomic UI (Staging)',
  apiUrl: 'https://api.staging.com/api'
};
